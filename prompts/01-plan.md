# Plan Prompt

Plan a native WinUI 3 invoice extraction demo where the app calls a local GitHub Copilot SDK bridge and falls back to deterministic sample data if SDK warm-up is not ready.

Return a minute-by-minute 45-minute run-of-show with explicit fallback branches, the live commands to run, and the cut lines to drop if you fall behind.