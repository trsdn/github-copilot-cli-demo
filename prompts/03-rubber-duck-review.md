# Rubber Duck Review

Review this invoice extraction schema. What breaks if the invoice has a foreign currency, missing tax, handwritten notes, line item totals that do not match subtotal, or multiple payment references?

Return a table with columns: case, current behavior, demo risk, mitigation.