# Multi-Model Compare

Run the same synthetic invoice through the available models and compare JSON validity, missing fields, line item accuracy, latency, and confidence.

Default to precomputed results for the live demo. Run live only if preflight is green and time allows. Output a comparison table.