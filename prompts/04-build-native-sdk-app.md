# Build Native SDK App

Implement the next smallest slice of the native WinUI invoice extractor. Keep SDK integration behind the local HTTP bridge and preserve the deterministic mock fallback. Do not change the demo flow and do not spawn visible console windows.

Make the smallest edit, then run the narrowest validation (a focused build or single test) immediately.